/* Under Maintenance */
/* Right Click Menu */
const contextMenu = document.getElementById("contextMenu");
window.addEventListener("contextmenu", e => { e.preventDefault(), contextMenu.style.display = "block", contextMenu.style.left = `${e.pageX}px`, contextMenu.style.top = `${e.pageY}px` }), document.addEventListener("click", () => { contextMenu.style.display = "none" });
const copyItem = document.getElementById("copy"),
   pasteItem = document.getElementById("paste"),
   shareItem = document.getElementById("share"),
   moreItem = document.getElementById("more");
copyItem.addEventListener("click", () => { console.log("Copy action clicked") }), pasteItem.addEventListener("click", () => { console.log("Paste action clicked") }), shareItem.addEventListener("click", () => { console.log("Share action clicked") }), moreItem.addEventListener("click", () => { console.log("More action clicked") });